#include<stdio.h>

int main()
{
		int ts_sz, i, A, B;
		scanf("%d", &ts_sz);
		for ( i = 0; i < ts_sz ; i++){
				scanf("%d %d", &A, &B);
		}
		for (i = 0; i<ts_sz; i++){
				printf("yes");
		}
		return 0;
}

