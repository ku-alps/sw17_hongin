#include <iostream>>
#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    int len = s.length();
    int answer = l;

    for (int l = 1 ; l <= len / 2; l++) {
       for (int i = 0 ; i < n - l) {
       }
    }
    return answer;
}

int main() {
    string s = "aabbaccc";
    cout << solution(s) << endl;
    return 0;
}
